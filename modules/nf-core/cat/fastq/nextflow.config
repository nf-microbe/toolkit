process {
    withName: RUNMERGE_FASTQ {
        publishDir = [
            path: { "${params.outdir}/RunMerge/cat/fastq" },
            mode: params.publish_dir_mode,
            pattern: "*.merged.fastq.gz",
            enabled: params.save_run_merged_fastq
        ]
    }
}