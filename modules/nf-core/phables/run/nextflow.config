process {
    withName: PHABLES_RUN {
        storeDir = { "${params.outdir}/AssemblyExtension/phables/run/${meta.id}_${meta.assembler}_${meta.assembly_method}" }
        ext.args = params.phables_args ? params.phables_args : ""
    }
}