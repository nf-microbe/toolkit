process {
    withName: COBRAMETA {
        storeDir = { "${params.outdir}/AssemblyExtension/cobrameta/${meta.id}_${meta.assembler}_${meta.assembly_method}" }
        ext.args = params.cobra_args ? params.cobra_args : ""
    }
}