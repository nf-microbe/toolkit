process {
    withName: CHECKV_ENDTOEND {
        storeDir = { "${params.outdir}/VirusCompleteness/checkv/endtoend/${meta.id}_${meta.assembler}_${meta.assembly_method}_${meta.extension}" }
        ext.args = params.checkv_args ? params.checkv_args : ""
    }
}