process {
    withName: LOGAN_UNITIGAWSCLIMULTIPLIER {
        storeDir = { "${params.outdir}/LoganDownload/logan/unitigawsclimultiplier/${meta.id}" }
        ext.args = params.logan_mult_min_abund ? "--min_abund ${params.logan_mult_min_abund}" : ""
    }
}