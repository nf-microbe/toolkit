process {
    withName: LOGAN_CONTIGAWSCLIMULTIPLIER {
        storeDir = { "${params.outdir}/LoganDownload/logan/contigawsclimultiplier/${meta.id}" }
        ext.args = params.logan_mult_min_abund ? "--min_abund ${params.logan_mult_min_abund}" : ""
    }
}