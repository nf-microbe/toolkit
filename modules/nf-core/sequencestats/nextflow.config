process {
    withName: SEQUENCESTATS {
        publishDir = [
            path: { "${params.outdir}/AssemblyQC/sequencestats/${meta.id}_${meta.assembler}_${meta.assembly_method}_${meta.extension}" },
            mode: params.publish_dir_mode,
            pattern: '*.seq_stats.tsv',
            enabled: true
        ]
    }
}