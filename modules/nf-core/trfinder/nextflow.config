process {
    withName: TRFINDER {
        storeDir = { "${params.outdir}/AssemblyQC/trfinder/${meta.id}_${meta.assembler}_${meta.assembly_method}_${meta.extension}" }
        ext.args = [
            params.trfinder_args ? "${params.trfinder_min_dtr}" : ""
        ].join(' ').trim()
    }
}